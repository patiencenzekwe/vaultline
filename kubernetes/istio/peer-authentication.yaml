apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: vaultline-mtls
  namespace: vaultline
spec:
  selector:
    matchLabels:
      app: vaultline-backend
  mtls:
    mode: STRICT
  portLevelMtls:
    3001:
      mode: PERMISSIVE